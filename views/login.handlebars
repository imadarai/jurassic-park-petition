<h1>This petition is about something I haven't thought about.  But this is going to be a tag line.</h1>

<img id="main-image" src="/images/main.png" alt="">

{{#if error}}
    <span class="error">Password is incorrect. Please try again.</span>
{{/if}}
{{#if wrongId}}
    <span class="error">Email doesn't exit.  Please try again or <a href="/register">Register</a></span>
{{/if}}

<div class="input-container">
    <form class="input" method="POST">
        <!-- Input Fiels -->
       <input type="email" name="email" placeholder="Email">
       <br>
       <input type="password" name="password" placeholder="Password">
       <br>
       <!-- BUTTONS -->
       <input type="hidden" name="_csrf" value="{{csrfToken}}">
       <!-- buttons -->
       <div class="button-container">
           <button id="register">LOGIN</button>
       </div>
    </form>
</div>
<h4>Not a member? <a href="/registration">Join here</a> </h4>
